# ドキュメントレビュー 2026-06-04 r2

判定: 合格
判定要約: 所見なし
根拠:
- 対象: branch `fix/bws-provisioning-inputs-issue-44` の current worktree（未コミット差分を含む）。`git status --short --branch` で README / spec / design / runbook / script と BWS provisioning 関連 Rust 差分を確認した。
- code doc comment: BWS provisioning の主要 use case entrypoint は、登録・更新用 BWS access token を `BwsAccessTokenInputPort` から hidden prompt / pipe で取得し、YubiKey storage を読まず、YubiKey に保存する復旧用 `bws-access-token` と同一値運用を前提にしないことを説明している（`rust/dotfiles-cli/src/secrets/application/run_register_gpg_backup_primary.rs:13`, `rust/dotfiles-cli/src/secrets/application/run_add_gpg_backup_spare.rs:14`, `rust/dotfiles-cli/src/secrets/application/run_provision_password_store_remote.rs:13`）。実装も `read_bws_access_token_for_provisioning()` を呼び、project name から ID を解決して create/update へ進むためコメントと矛盾しない。
- port / adapter doc comment: `BwsAccessTokenInputPort` は登録・更新用 token を YubiKey へ保存しない境界として説明し、caller が同一値運用を前提にしてはならないと明記している（`rust/dotfiles-cli/src/secrets/ports/io.rs:41`）。BWS adapter は SDK field `organization_id` を adapter-local な request scope として扱い、利用者に Bitwarden organization 入力を要求しないと説明している（`rust/dotfiles-cli/src/secrets/adapters/bw.rs:302`）。これは実装が access token から scope ID を取得して SDK request にだけ渡す形と整合する。
- `verify-yubikey --check bws`: comment は typed port で `gpg-secret-key-backup` envelope を取得し、schema / `metadata.primary_fingerprint` / ciphertext 構造検証、接続中 YubiKey recipient matching、unwrap-free recoverability、`password-store-remote` typed 取得までを BWS check 成功条件にすると説明している（`rust/dotfiles-cli/src/secrets/application/run_verify_yubikey_with.rs:162`）。実装も project / secret を一意解決し、`fetch_gpg_backup_envelope`、`resolve_connected_recipient`、`envelope.resolve_recipient`、`fetch_password_store_remote` を順に実行しており、BWS secret 取得成功だけを完了扱いにしていない（`rust/dotfiles-cli/src/secrets/application/run_verify_yubikey_with.rs:179`）。
- README / runbook / spec / design: `dotfiles gpg export-ssh-public-key --primary-fingerprint <40-hex-fingerprint>` の必須 option は README、runbook、spec、GnuPG/SSH design で一致している（`README.md:86`, `docs/secret-recovery/initial-provisioning-runbook.md:29`, `docs/secret-recovery/secret-recovery-spec.md:108`, `docs/secret-recovery/gnupg-ssh-design.md:109`）。前回 finding の option 欠落は解消されている。
- project gate: README / runbook / BSM design は project `dotfiles-secret-recovery` を手動作成し、`dotfiles` / `scripts/provision-secret-recovery-source.sh` が project を作成せず、登録・更新用 token から同名 project が 1 件だけ見えることを provisioning 前 gate とする説明で一致している（`README.md:111`, `docs/secret-recovery/initial-provisioning-runbook.md:34`, `docs/secret-recovery/initial-provisioning-runbook.md:85`, `docs/secret-recovery/bitwarden-secrets-manager-design.md:42`, `docs/secret-recovery/bitwarden-secrets-manager-design.md:118`）。script も同じ gate を pause 文で確認してから `pass-remote register` / `gpg-backup register` を呼ぶ（`scripts/provision-secret-recovery-source.sh:195`）。
- 旧 token / rotate: runbook は新 token 有効化、全 YubiKey への反映、`verify-yubikey --check bws` による確認後に旧 token を失効させる順序を示している（`docs/secret-recovery/initial-provisioning-runbook.md:121`）。BSM design も古い token は全 YubiKey 更新後に失効させると説明しており、現行の rotate 仕様と矛盾しない（`docs/secret-recovery/bitwarden-secrets-manager-design.md:136`）。
- organization / machine / service account / `dotfiles-secret-recovery-reader`: `rg -n "ProvisioningAccessTokenInputPort|dotfiles-secret-recovery-reader|machine account|service account|read-only reader|reader token|旧 token|old token" README.md docs/secret-recovery rust/dotfiles-cli/src scripts/provision-secret-recovery-source.sh -S` を実行し、旧 user-facing 前提は検出されなかった。`organization` は GitHub owner 名の説明または SDK 内部 field 名の説明に限定され、Bitwarden organization / machine account / service account / `dotfiles-secret-recovery-reader` 前提を user-facing に戻していない。
