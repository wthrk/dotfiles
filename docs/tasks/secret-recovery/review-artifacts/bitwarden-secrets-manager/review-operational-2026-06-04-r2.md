# 運用整合レビュー（Bitwarden Secrets Manager / fix/bws-provisioning-inputs-issue-44）2026-06-04 r2

判定: 合格
判定要約: 所見なし
根拠:
- 対象は `/Users/ya/works/dotfiles` の current worktree（branch `fix/bws-provisioning-inputs-issue-44`、未コミット差分を含む）とし、active work item は `docs/tasks/secret-recovery/work-items/bitwarden-secrets-manager.md` として確認した。直接確認対象は `scripts/provision-secret-recovery-source.sh`、`README.md`、`docs/secret-recovery/README.md`、`docs/secret-recovery/secret-recovery-spec.md`、`docs/secret-recovery/bitwarden-secrets-manager-design.md`、`docs/secret-recovery/initial-provisioning-runbook.md`、および BWS provisioning / verify / update 経路の Rust 実装である。
- 前回 finding の `dotfiles-secret-recovery` project 作成/既存化 gate は、現行差分で手動 gate と実装停止条件に分解されている。runbook は project 作成を **[手動]** として対応表に置き、Phase A 手順 7 で「project を作成し、登録・更新用 token から同名 project が 1 件だけ見える状態」を BWS provisioning 前 gate として固定している（`docs/secret-recovery/initial-provisioning-runbook.md:34`、`:84`-`:90`）。README も同じく、実行前に project を作成し、`dotfiles` / script は project を作成せず、未存在または複数件なら停止すると明示している（`README.md:111`）。
- script 側も project を自動作成する実装ではなく、BWS 登録段階の直前に operator へ project 作成済み・一意可視状態を確認させる `pause` を置いている（`scripts/provision-secret-recovery-source.sh:194`-`:196`）。その後の `pass-remote register` と `gpg-backup register` は dotfiles CLI を通じて project name から ID を解決するため、operator の手動確認だけで secret 作成へ進む構造ではない（同 `:198`-`:202`）。
- Rust 側の強制条件として、BWS project は `BwsProjectName::DOTFILES_SECRET_RECOVERY.resolve_id(...)` で一意解決され、0 件なら `bws project not found: dotfiles-secret-recovery`、複数件なら `multiple bws projects matched: dotfiles-secret-recovery` の domain failure で停止する（`rust/dotfiles-cli/src/secrets/domain/bws.rs:119`-`:131`）。provisioning の各 use case は secret 登録・更新前にこの project 解決を実行している（`rust/dotfiles-cli/src/secrets/application/run_provision_password_store_remote.rs:49`-`:53`、`rust/dotfiles-cli/src/secrets/application/run_register_gpg_backup_primary.rs:58`-`:62`、`rust/dotfiles-cli/src/secrets/application/run_add_gpg_backup_spare.rs:67`-`:78`）。
- token モデルは実行手順上監査可能に分離されている。design は provisioning 用の登録・更新 token と YubiKey 保存用の復旧 token を同一値にしないと定義し、project 作成は手動 gate としている（`docs/secret-recovery/bitwarden-secrets-manager-design.md:41`-`:47`、`:116`-`:123`）。script は `PROVISIONING_BWS_TOKEN` と `RECOVERY_BWS_TOKEN` を別変数・別入力として扱い、前者を BWS 書込み command へ pipe し、後者を `yubikey put bws-access-token --stdin` へ pipe している（`scripts/provision-secret-recovery-source.sh:99`-`:120`、`:196`-`:209`）。
- BWS 更新系 gate は stale overwrite prevention と明示確認で強制される。`pass-remote register` は既存 secret が 1 件ある場合だけ guard を取得し、上書き確認後に `update_password_store_remote_if_unchanged` へ guard を渡す（`rust/dotfiles-cli/src/secrets/application/run_provision_password_store_remote.rs:77`-`:100`）。`gpg-backup add-spare` も envelope と guard を取得し、確認後に DEK unwrap / spare wrap を行い、guard 一致を条件に更新する（`rust/dotfiles-cli/src/secrets/application/run_add_gpg_backup_spare.rs:80`-`:126`）。adapter は更新直前に現行 secret を再取得して guard を照合する（`rust/dotfiles-cli/src/secrets/adapters/bw.rs:161`-`:199`、`:245`-`:282`）。
- `verify-yubikey --check bws` は BWS secret の単純取得成功だけを gate にしていない。local storage 検証後、BWS project / secret を解決し、`gpg-secret-key-backup` envelope を取得して schema / fingerprint / recipient matching を domain で検証し、接続中 YubiKey recipient が一致することを確認したうえで、`password-store-remote` も取得する（`rust/dotfiles-cli/src/secrets/application/run_verify_yubikey_with.rs:57`-`:83`、`:162`-`:200`）。この外部確認は unwrap を呼ばずに recoverability を確認するため、復旧可能性 gate と secret 平文化の境界が分離されている。
- 実行手順と文書の範囲分離も監査可能である。runbook は command 実現済みの工程と手動工程を対応表と Phase A / B で分け、Bitwarden account 2FA、GPG secret key 作成、各サービスの FIDO2 登録、BWS project 作成は手動として扱っている（`docs/secret-recovery/initial-provisioning-runbook.md:20`-`:39`、`:47`-`:90`、`:105`-`:121`）。`docs/secret-recovery/README.md` には新規 runbook への導線が追加され、実行手順の入口が追跡できる（`docs/secret-recovery/README.md:7`-`:12`）。
- role separation / gate / completion logic は、対象 work item が未コミット worktree を含む対象差分の再特定、実質条件に対応する確認結果、BSM 実装義務・更新系義務、必須レビュー担当集合と集約合格を完了条件として要求しており、補助記録同期だけを gate にしていない（`docs/tasks/secret-recovery/work-items/bitwarden-secrets-manager.md:39`-`:48`）。現行レビュー記録群は個別レビューとして存在するが、本判定は過去レビュー結果や他担当判定を代替根拠にせず、上記の script / docs / Rust 実体を直接確認して行った。
- 補助確認として `bash -n scripts/provision-secret-recovery-source.sh` と `git diff --check` は成功した。旧 token model / machine account 前提の残存検索では、今回の運用 gate を崩す user-facing project 自動作成前提や reader token 前提は確認されなかった。
