# Bitwarden Secrets Manager security review 2026-06-04 r2

判定: 合格
判定要約: 所見なし
根拠:
- 対象: current worktree on `fix/bws-provisioning-inputs-issue-44`。未コミット worktree を含む `git status --short --branch` と `git diff --name-status HEAD` で対象差分を確認した。過去レビュー結果は判定代替にせず、現行コード・文書・script を直接確認した。
- 必須参照: `docs/task-governance/security-obligations.md`、`docs/task-governance/implementation-review-judgement.md`、`docs/tasks/secret-recovery/work-items/bitwarden-secrets-manager.md`、`docs/architecture/hexagonal-implementation-rules.md`、`docs/secret-recovery/secret-handling.md`、`docs/secret-recovery/secret-recovery-spec.md`、`docs/secret-recovery/bitwarden-secrets-manager-design.md`、`docs/secret-recovery/gnupg-ssh-design.md`、`docs/secret-recovery/initial-provisioning-runbook.md`、`README.md` を確認した。
- 層分類: `rust/dotfiles-cli/src/secrets/entrypoint/dispatch.rs` は entrypoint、`rust/dotfiles-cli/src/secrets/application/run_*.rs` は application、`rust/dotfiles-cli/src/secrets/domain/{bws,commands,verification}.rs` は domain、`rust/dotfiles-cli/src/secrets/ports*.rs` と `ports/{bw,io}.rs` は port、`rust/dotfiles-cli/src/secrets/adapters/**` は adapter、`rust/dotfiles-cli/src/secrets/support/protection/bws.rs` は support、`rust/dotfiles-cli/tests/secrets_cli.rs` は tests、`scripts/provision-secret-recovery-source.sh` は shell entrypoint、`README.md` と `docs/secret-recovery/**` は文書差分として確認した。各層で secret 平文 public API 化、SDK 型の domain 漏れ、support への固定 secret lookup / external check plan 移動は確認されなかった。
- BWS access token 入力・argv/env/xtrace: Rust provisioning path は `BwsAccessTokenInputPort` で hidden prompt / stdin pipe から `ProtectedSecret` へ読み、`rust/dotfiles-cli/src/secrets/adapters/io/process.rs:66`-`78` は argv/env 入力を持たない。`scripts/provision-secret-recovery-source.sh:16`-`20` は `set +x` と `unset` trap を置き、`scripts/provision-secret-recovery-source.sh:99`-`120` は provisioning token と recovery token を別々に stdin pipe で `dotfiles` へ渡す。token 値の stdout/stderr/log/argv/env/xtrace 露出経路は確認されなかった。
- token 分離コメント: 前回 finding 対象だった Rust コメントは、`rust/dotfiles-cli/src/secrets/ports/io.rs:40`-`47`、`rust/dotfiles-cli/src/secrets/domain/commands.rs:286`-`289`、`rust/dotfiles-cli/src/secrets/application/run_provision_password_store_remote.rs:23`-`26`、`rust/dotfiles-cli/src/secrets/application/run_register_gpg_backup_primary.rs:22`-`25`、`rust/dotfiles-cli/src/secrets/application/run_add_gpg_backup_spare.rs:22`-`25` で、BWS 登録・更新用 token を YubiKey に保存せず、YubiKey 保存用 `bws-access-token` は別経路の read 用最小権限 token とする説明へ統一されている。
- BWS SDK 境界: `rust/dotfiles-cli/src/secrets/support/protection/bws.rs:77`-`104` は SDK login に必要な owned plaintext token buffer を protection 境界内で作成し、`ZeroizingAccessTokenLoginRequest` の Drop で zeroize する。`rust/dotfiles-cli/src/secrets/adapters/bw.rs:61`-`69`、`86`-`93`、`110`-`122`、`170`-`198`、`254`-`280` は SDK error を固定文言へ map し、token / secret value を error に含めていない。
- YubiKey storage: `rust/dotfiles-cli/src/secrets/adapters/yubikey/storage_adapter.rs:103`-`145` は保存時に `ProtectedSecret` を sealed storage へ渡し、読取時も `SecretSession` / `ProtectedSecret` で扱う。`rust/dotfiles-cli/src/secrets/application/run_verify_yubikey_with.rs:57`-`116` は local storage 検証後に `bws-access-token` を BWS check 分岐内で on-demand に読み、分岐外へ保持しない。
- GPG backup envelope / BWS check: `rust/dotfiles-cli/src/secrets/domain/gpg_backup.rs:137`-`244` は envelope schema、lowercase hex 40 文字 fingerprint、nonce/body/tag、recipient serial + public key fingerprint 両一致を domain 検証し、parse error に secret 本文を含めない。`rust/dotfiles-cli/src/secrets/application/run_verify_yubikey_with.rs:162`-`200` は `gpg-secret-key-backup` と `password-store-remote` の取得、envelope schema、接続中 YubiKey recipient matching、unwrap-free recoverability を確認し、`unwrap_dek` を呼ばない。
- GitHub/BWS 操作と private repo 所在出力: 前回 finding 対象だった `scripts/provision-secret-recovery-source.sh` は、repo 作成ログを固定文言にし（`scripts/provision-secret-recovery-source.sh:169`-`172`）、`pass git push` の stdout/stderr を抑止して失敗時も固定文言へ変換している（`scripts/provision-secret-recovery-source.sh:181`-`185`）。`password-store-remote` は design 上 provisioning 入力では `--url` を許可される非 credential 値だが、通常ログ・エラー本文・診断出力へ clone URL / owner/repo を出す経路は確認されなかった。
- README/runbook/spec/design: `README.md`、`docs/secret-recovery/secret-recovery-spec.md`、`docs/secret-recovery/bitwarden-secrets-manager-design.md`、`docs/secret-recovery/initial-provisioning-runbook.md` は、BWS 登録・更新用 token と YubiKey 保存用復旧 token の分離、token の argv/log/shell history/env/temp file 禁止、`gpg-secret-key-backup` の encrypted envelope、`password-store-remote` の出力禁止を明記している。
- committed secret material: `rg` による差分追加行と対象 tree の検索で、実 BWS token、GitHub token、private key material、GPG secret key block の混入は確認されなかった。hit は仕様文言、固定 secret 名、script 変数名、test fixture のダミー値に限られる。`git diff --check HEAD` は whitespace error なし。
