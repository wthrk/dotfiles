判定: 不合格
判定要約: `support/protection` が BWS secret value の取得後に domain 型生成と URL domain rule 適用まで担っており、support 層の責務境界を越えている。
根拠:
- レビュー対象: 現行ブランチ `fix/bws-provisioning-inputs-issue-44` の未コミット worktree を含む current worktree。過去レビュー記録は判定根拠に使わず、現行コードと正本文書を直接確認した。
- 適用文書: `docs/architecture/hexagonal-implementation-rules.md`, `docs/architecture/review-checklist.md`, `docs/task-governance/implementation-review-judgement.md`, `docs/secret-recovery/secret-handling.md`, `docs/secret-recovery/bitwarden-secrets-manager-design.md`。
- 層哲学への回答:
  - application: `rust/dotfiles-cli/src/secrets/application/run_register_gpg_backup_primary.rs`, `run_add_gpg_backup_spare.rs`, `run_provision_password_store_remote.rs`, `run_verify_yubikey_with.rs` の BWS 経路は、project/secret 候補取得、domain の一意解決、取得・確認・作成・更新の順序制御を行っている。これは use case の順序・分岐・停止条件であり、BWS SDK 型や UUID 型変換を直接扱っていないため、この部分自体は application 責務に収まる。
  - domain: `rust/dotfiles-cli/src/secrets/domain/bws.rs` は固定 project/secret name、lookup 候補、0 件/複数件の failure 化を domain rule として表現している。これは BWS SDK を差し替えても変わらない対象同一性・一意性の規則であり、domain 境界に置くべき処理である。
  - ports: `rust/dotfiles-cli/src/secrets/ports/bw.rs` は BWS project/secret 候補取得、typed secret 取得、create/update の capability を宣言しており、SDK 型や UUID 型は公開していない。`ProtectedSecret` は secret carrier として許可される範囲に留まる。
  - adapters: `rust/dotfiles-cli/src/secrets/adapters/bw.rs` の real adapter は SDK の list/get/create/update と port 境界型の変換を担う。`BwsProjectName` / `BwsSecretName` の一意解決は application/domain 側で行われ、adapter は候補列挙や SDK request 変換に寄っている。adapter 配下の `#[cfg(feature = "secrets-internal-test-stub")]` stub は production build 非混入、compile-time selection、stdout sentinel observation、BWS/YubiKey state 分離の条件を満たすため、stub の存在自体は不合格根拠にしていない。
  - support: `rust/dotfiles-cli/src/secrets/support/protection/bws.rs` は SDK login の owned plaintext token buffer 作成、SDK 呼び出し、zeroize、core dump 抑止を閉じる部分については secret 保護境界の dedicated backend 操作である。しかし同 file は `PasswordStoreRemote` domain 型に依存し、SDK get で得た `password-store-remote` value に対して domain validation まで実行しているため、「secret 保護境界の backend 操作または業務判断を含まない変換だけ」という support の哲学に違反している。
- Finding 1: [rust/dotfiles-cli/src/secrets/support/protection/bws.rs](/Users/ya/works/dotfiles/rust/dotfiles-cli/src/secrets/support/protection/bws.rs:13) が `domain::pass_restore::PasswordStoreRemote` を import しており、support 層が domain 層の業務語彙へ依存している。`docs/architecture/review-checklist.md` の support 依存方向は「言語標準ライブラリと外部技術 crate のみ」であり、BWS 専用 protection 操作の例外も SDK 呼び出し・平文 buffer 作成・zeroize を同じ境界に閉じるためのもので、domain 型生成を support へ置く許可ではない。
- Finding 2: [rust/dotfiles-cli/src/secrets/support/protection/bws.rs](/Users/ya/works/dotfiles/rust/dotfiles-cli/src/secrets/support/protection/bws.rs:65) の `get_password_store_remote` は SDK `SecretGetRequest` 実行後、`PasswordStoreRemote::parse` により `git@github.com:<owner>/<repo>.git` 形式の domain rule を適用して domain 値を返している。この処理は「SDK 返却 buffer を zeroize 管理に入れる」までは support technical primitive だが、clone URL の妥当性判断と domain 型生成は support の backend 技術境界ではない。`docs/secret-recovery/bitwarden-secrets-manager-design.md` も `support/protection` に置けるものを access token の平文借用、SDK 呼び出し直前の owned plaintext buffer 作成、SDK 呼び出しの安全な完了、zeroize、業務判断を含まない外部 API 型変換に限っている。
- Finding 3: [rust/dotfiles-cli/src/secrets/adapters/bw.rs](/Users/ya/works/dotfiles/rust/dotfiles-cli/src/secrets/adapters/bw.rs:125) は `fetch_password_store_remote` の翻訳境界を support の `get_password_store_remote` へ委譲しており、adapter が担うべき「外部 API 返却値を port/domain 境界型へ翻訳する」責務が support 側へ押し出されている。これは `adapter を薄くするために support へ逃がすことは禁止する` という構造規約に反する。
- checklist 確認:
  - adapters 配下の公開シンボルを `rg` で列挙した。`pub(in crate::secrets)` / `pub(super)` の adapter 型は port trait 実装型または内部 backend 型であり、今回の不合格根拠は公開面ではない。
  - application 配下に `println!` / stdin read / Bitwarden SDK 型 import は確認していない。BWS 経路の private helper は `run_bws_check` が外部確認 plan の順序、`resolve_remote_url` が入力優先順位と domain parse の停止条件を担っており、いずれも application 側の順序制御として説明可能である。
  - internal backend stub は adapter-local feature gate で production build に含まれず、integration test 側は stub module を import せず feature 有効の `dotfiles` binary を実行して stdout sentinel の最終観測を読む構成である。BWS/YubiKey port stub は state/schema/file を共有していない。
- 必要な修正: `support/protection/bws.rs` から `PasswordStoreRemote` 依存と `PasswordStoreRemote::parse` を除去し、support は SDK 呼び出し・plaintext buffer の一時所有・zeroize/core dump 保護に限定すること。BWS secret value から `PasswordStoreRemote` への domain rule 適用は、adapter の port 実装または規定済みの domain/application 境界で責務分類し直して配置すること。
