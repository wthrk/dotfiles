# Git レビュー記録

この文書は `docs/tasks/secret-recovery/tasks.md` の作業項目 `Git` に対する固定実装単位 `レビュー` の記録先である。

## 実装担当からの引き継ぎ

- レビュー状態: `完了（現行サイクル集約レビュー合格）`
- 対象ブランチ: `feat/secrets-restore-pass-issue-15`
- 対象差分識別子: `a188d3d..35232c8`（base `a188d3d` は #14 統合後、HEAD `35232c8`。#15 restore-pass 実装）
- 実装側確認証跡: `./confirmation.md`

## レビュー担当チェック項目

1. 対象作業項目のスコープ外へ越境していないこと。
2. 責務境界と依存方向が [レビュー観点チェックリスト](../../../../architecture/review-checklist.md#レビュー観点チェックリスト構造) の観点に適合していること。
3. 対象 work-item の `レビュー合格条件` を満たすこと。
4. 仕様・設計・作業定義文書の要求挙動、停止条件、成功条件が反映されていること。
5. 文書構造の整合だけでなく、必須の実行手順・役割分離・ゲート条件・証跡要件・完了判定ロジックについて、実運用での強制可能性または監査可能性に具体的懸念がないこと。懸念がある場合は必ず所見化し、強制可能性/監査可能性が不確実なまま `合格` にしないこと（`スコープ外` や `運用徹底` を理由に格下げしない）。
6. 具体的懸念、残留リスク、未解消疑義、要追跡事項、運用依存の注意事項を記録した場合、それは `finding あり` であり、`No findings` / `指摘なし` / `懸念なし` / `合格` と併記しないこと。判定は少なくとも `要修正` とし、解消条件または差戻し事項を同じ記録に書くこと。

## 判定記録フォーマット

- 各レビュー担当の記録は、必ず次の順で書く。
  1. `判定: <合格|要修正|不合格>`
  2. `判定要約: <所見なし|主要論点要約>`
  3. `根拠:`
- `判定` に使ってよいラベルは `合格`、`要修正`、`不合格` のみとする。
- `通しません`、`No findings`、`指摘なし`、`no blockers`、`pass` などの自由文を `判定` の代わりに使ってはならない。
- `合格` の場合、`判定要約` は `所見なし` とする。
- `要修正` または `不合格` の場合、`判定要約` は主要論点を 1 行で要約し、`根拠:` に差戻し条件または不合格理由を箇条書きで記録する。
- 集約判定も同じ構造で書き、`集約後レビュー判定`、`集約判定要約`、`集約根拠` を必ず揃える。

## セキュリティ所見記録（必須）

### 1) 秘密値・認証情報の扱い

- 確認状態: `完了`
- 確認対象（ファイル/経路）: `.gpg-id` 読取り、password-store entry 読取り、clone 経路、平文バッファ
- 所見: 平文は zeroize 対象で非露出。`.gpg-id`/store entry の読取りは symlink/TOCTOU を std-only の dev/ino 照合で閉鎖（libc/OS 数値ハードコード不使用）。
- 差戻し要否: `不要`
- 未実施理由（未実施時のみ）: `該当なし`

### 2) 漏えい経路（ログ/引数/一時ファイル/stdout/stderr）

- 確認状態: `完了`
- 確認対象（出力経路）: clone URL、エラー出力、平文経路
- 所見: 平文は zeroize され stdout/stderr/ログへ非露出。URL 検証あり、host key pin あり。
- 差戻し要否: `不要`
- 未実施理由（未実施時のみ）: `該当なし`

### 3) 権限境界・永続化・失敗時挙動

- 確認状態: `完了`
- 確認対象（境界/保存先/失敗経路）: clone 原子性、読取り size cap、失敗時状態
- 所見: clone は原子的に行われ、読取りは size cap・symlink 拒否で保護。失敗時状態は識別可能で再実行安全。
- 差戻し要否: `不要`
- 未実施理由（未実施時のみ）: `該当なし`

## 役割別レビュー記録（レビュー担当記入）

各役割の判定は、現行サイクル最終 HEAD `35232c8` に対する再レビュー後の結果である。

### 構造レビュー担当

- 判定: `合格`
- 判定要約: `所見なし`
- 根拠:
  - Git 操作は port/adapter 境界に閉じ、application は手順と停止条件のみ、domain はインフラ非依存。
  - internal backend stub は compile-time feature・同一 production path・port 契約維持であり、不合格根拠にならない。

### 運用整合レビュー担当

- 判定: `合格`
- 判定要約: `所見なし`
- 根拠:
  - 手順・停止条件・エラー時状態・再実行安全性が強制可能・監査可能。
  - README 整合。

### セキュリティレビュー担当

- 判定: `合格`
- 判定要約: `所見なし`
- 根拠:
  - `.gpg-id`/store entry 読取りの symlink/TOCTOU を std-only dev/ino 照合で閉鎖、libc/OS 数値ハードコード不使用。
  - host key pin、平文 zeroize・非露出、URL 検証、clone 原子性。

### 仕様適合レビュー担当

- 判定: `合格`
- 判定要約: `所見なし`
- 根拠:
  - spec L174 の restore-pass 手順に厳密一致。
  - 設計外の identity 事前検証/sole-identity/fingerprint 再取得の踏み越えは撤去済み。design L83 違反の鍵リング全走査なし。

### テストレビュー担当

- 判定: `合格`
- 判定要約: `所見なし`
- 根拠:
  - restore-pass 手順・停止条件・安全読取り（symlink 拒否・size cap）のテストを網羅。
  - internal stub は責務基準で適切（generic test double として機械的に不合格判定しない）。

### ドキュメントレビュー担当

- 判定: `合格`
- 判定要約: `所見なし`
- 根拠:
  - コメントが実装と整合し why/責務境界を説明。
  - 撤去済み機構への陳腐化記述なし。

### アーキテクチャ整合レビュー担当

- 判定: `合格`
- 判定要約: `所見なし`
- 根拠:
  - restore-pass 経路全体が一貫設計、責務が層をまたいで一貫分配。
  - dev/ino の 2 箇所配置は別責務であり各層に正当。

### 起動不能役割がある場合の記録参照

- 記録参照: `該当なし（必須7担当すべて fresh subagent で起動・判定取得）`

## 差し戻し履歴（現行サイクル内）

- (1) 初回ゲートで仕様適合/セキュリティ/ドキュメントが `要修正`（`clone_adapter` module doc 陳腐化、読取り TOCTOU）→ remediation `56f2309` → 再レビュー合格。
- (2) O_NOFOLLOW 移植性指摘＋libc 直呼び禁止 → std-only dev/ino 照合へ remediation `35232c8` → 再レビュー（セキュリティ・構造・ドキュメント・テスト）合格、仕様適合・アーキテクチャ整合・運用整合は手順/仕様/アーキ不変として引継ぎ。

## 集約判定

- 集約後レビュー判定: `合格`
- 集約判定要約: `所見なし`
- 集約根拠:
  - 必須7担当（構造・仕様適合・セキュリティ・テスト・ドキュメント・アーキテクチャ整合・運用整合）が現行サイクル最終 HEAD `35232c8` に対する再レビューで全員 `合格`、未解消 finding なし。
  - restore-pass 手順は spec L174 へ直接照合し未解消なし。設計外踏み越えは撤去済みで境界維持。
  - 読取りの symlink/TOCTOU は std-only dev/ino 照合で閉鎖（libc/OS 数値ハードコード不使用）、平文 zeroize・非露出。
  - 各担当は対象コードを独立に直接読んで判定。
- 差戻し事項: `なし（解消済み。履歴は「差し戻し履歴」節を参照）`
- 後続対応状態: `不要（未解消 finding なし）`
- 懸念/残留リスク/未解消疑義/要追跡事項/運用依存の注意事項が1件でも残る場合は `合格` を記録しない。
- 後続対応メモ: `未解消 finding なし。後続対応は不要。`

## 確認記録（実検証）

実検証の最小記録は `./confirmation.md` に記載する。実装担当が dev shell で `cargo build` / `cargo test`（186 unit + 33 secrets-internal-test-stub integration）/ `cargo clippy --all-targets -- -D warnings` / `cargo fmt --check` / `cargo xtask check` をすべて通過。restore-gpg(#14) 退行なし。対象差分識別子: `a188d3d..35232c8`（branch `feat/secrets-restore-pass-issue-15`）。
