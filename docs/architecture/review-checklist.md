# レビュー観点チェックリスト（構造）

この文書は、アーキテクチャ規約に対する構造レビュー観点の正本である。

## 構造境界の観点

1. `入口` / `アプリケーション` / `ドメイン` / `ポート` / `アダプター` / `補助` / `テスト` の責務境界が維持されていること。
2. `アダプター` にユースケースの順序制御が混入していないこと。
3. `アプリケーション` に具体 I/O 実装が混入していないこと。
4. `補助` に業務語彙や機能固有語彙が混入していないこと。
5. `ポート` に要約・報告データや利用者向け文言が混入していないこと。
